<div class="card">
    <div class="card-body">
        <h5 class="card-title">Projeto: <?= htmlspecialchars($project->title) ?></h5>

        <?php if ($project->errors('title')): ?>
            <div class="alert alert-danger p-2"><?= $project->errors('title') ?></div>
        <?php endif; ?>
    </div>
</div>

<hr>

<div class="card mt-4">
    <div class="card-header">
        Arquivos do Projeto (Resultados)
    </div>
    <div class="card-body">
        
        <ul class="list-group list-group-flush">
            <?php if (empty($arquivos)): ?>
                <li class="list-group-item">Nenhum arquivo enviado para este projeto.</li>
            <?php endif; ?>

            <?php foreach ($arquivos as $arquivo): ?>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <a href="<?= htmlspecialchars($arquivo->getPublicPath()) ?>" target="_blank">
                        <?= htmlspecialchars($arquivo->nome_original) ?>
                    </a>
                    
                    <?php if ($this->isAdmin()): ?>
                        <form action="<?= route('arquivos.destroy', ['id' => $arquivo->id]) ?>" method="POST" style="display:inline;">
                            <input type="hidden" name="_method" value="DELETE">
                            <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Tem certeza?')">
                                Remover
                            </button>
                        </form>
                    <?php endif; ?>
                </li>
            <?php endforeach; ?>
        </ul>

        <?php if ($this->isAdmin()): ?>
            <hr>
            <h5 class="card-title mt-3">Anexar novo arquivo</h5>
            
            <?php require __DIR__ . '/../layouts/_flash_messages.phtml'; ?>

            <form action="<?= route('arquivos.store', ['id' => $project->id]) ?>" method="POST" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="arquivo" class="form-label">Arquivo (Max 2MB, tipos: JPEG, PNG, PDF):</label>
                    <input type="file" name="arquivo" id="arquivo" class="form-control">
                    
                    <?php if ($project->errors('arquivo')): ?>
                        <div class="text-danger mt-1" style="font-size: 0.9em;">
                            <?= $project->errors('arquivo') ?>
                        </div>
                    <?php endif; ?>
                </div>
                <button type="submit" class="btn btn-primary">Enviar Arquivo</button>
            </form>
        <?php endif; ?>

    </div>
</div>

<a href="<?= route('projects.index') ?>" class="btn btn-secondary mt-3">Voltar para Lista</a>